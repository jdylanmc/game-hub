# Shared reviewer v2 activation boundary

This directory contains the complete, fingerprinted shared reviewer v2
configuration. It is dormant during the #56 v1 bootstrap: active v1 workflow
files must never reference these paths.

Issue #58 owns the explicit activation transaction, a fresh matching real
Azure calibration, protected attestation, and exact-head fan-in proof.
